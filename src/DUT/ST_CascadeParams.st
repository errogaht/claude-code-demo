(*
    Параметры настройки каскадного управления
    Задаются оператором на панели ШУО
*)
TYPE ST_CascadeParams :
STRUCT
    (* === Уставки давления === *)
    rPressureSetpoint       : REAL := 0.8;      // Уставка давления в общем паропроводе, МПа
    rPressureTolerance      : REAL := 0.02;     // Допустимое отклонение давления, МПа (±0.02)

    (* === Уставки мощности для подключения/отключения === *)
    rPowerConnectThreshold  : REAL := 3.5;      // Мощность основного котла для подключения резервного, МВт
    rPowerDisconnectThreshold: REAL := 1.5;     // Мощность основного котла для отключения резервного, МВт

    (* === Уставки времени, секунды === *)
    tConnectDelay           : TIME := T#60S;    // Выдержка времени для подключения резервного котла
    tDisconnectDelay        : TIME := T#60S;    // Выдержка времени для отключения резервного котла
    tStabilizationTime      : TIME := T#120S;   // Время стабилизации после включения/отключения

    (* === Параметры управления резервными котлами === *)
    rReservePowerSetpoint   : REAL := 2.5;      // Уставка мощности для резервных котлов, МВт
    rPowerRampRate          : REAL := 0.1;      // Скорость изменения мощности, МВт/сек

    (* === Границы мощности горелки === *)
    rMinPower               : REAL := 1.0;      // Минимальная мощность горелки, МВт
    rMaxPower               : REAL := 4.0;      // Максимальная мощность горелки, МВт

END_STRUCT
END_TYPE
