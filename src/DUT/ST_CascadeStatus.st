(*
    Состояние каскадного управления
    Отображается на панели оператора ШУО
*)
TYPE ST_CascadeStatus :
STRUCT
    (* === Основные состояния === *)
    bEnabled                : BOOL;             // Каскадное управление включено
    bStabilizing            : BOOL;             // Идет стабилизация (блокировка изменений)
    bConnecting             : BOOL;             // Идет процесс подключения резервного котла
    bDisconnecting          : BOOL;             // Идет процесс отключения резервного котла

    (* === Информация о котлах === *)
    nMasterBoilerIndex      : INT := -1;        // Индекс основного котла (0-3, -1 = нет)
    nActiveBoilersCount     : INT;              // Количество работающих котлов
    nAvailableBoilersCount  : INT;              // Количество доступных котлов

    (* === Показания общего паропровода === *)
    rCommonPressure         : REAL;             // Давление в общем паропроводе, МПа
    rTotalPower             : REAL;             // Суммарная мощность работающих котлов, МВт

    (* === Таймеры (оставшееся время) === *)
    tConnectRemaining       : TIME;             // Осталось до подключения резервного
    tDisconnectRemaining    : TIME;             // Осталось до отключения резервного
    tStabilizeRemaining     : TIME;             // Осталось до окончания стабилизации

    (* === Диагностика === *)
    nLastError              : INT;              // Код последней ошибки
    sLastErrorMsg           : STRING(80);       // Сообщение последней ошибки

END_STRUCT
END_TYPE


(* Коды ошибок *)
(*
    0   - Нет ошибок
    1   - Нет доступных котлов
    2   - Основной котел потерял связь
    3   - Основной котел неисправен
    4   - Основной котел переведен в местный режим
    5   - Потеря связи с резервным котлом
    6   - Неисправность резервного котла
*)
